import { describe, expect, it } from 'vitest'

function createNode(val: number) {
  return { sortIndex: val }
}

describe('test min heap', () => {
  it('empty heap case', () => {
    const tasks = []
  })
  it('min heap length === 1 case', () => {

  })
  it('min heap case', () => {

  })
})